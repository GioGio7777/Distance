public class main {

    double calculateMinkowski(double x[], double y[], double P1, double P2, double R) {
        double result = 0;
        double plus = 0;

        for (int i = 0; i < x.length; i++) { // For sigma in equation
            plus += (x[0] - y[0]);

            Math.pow(plus, R);

        }

        return Math.pow(result, 1 / R);
    }

    double calculateMahalonobis(double x[], double y[], double P1, double P2) {
        double result = 0;


        return result;
    }

    public static void main(String[] args) {

    }
}
